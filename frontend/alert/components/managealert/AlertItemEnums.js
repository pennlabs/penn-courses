// Enums for AlertItem component
export const AlertAction = Object.freeze({
    Resubscribe: "resub",
    Cancel: "cancel",
    Delete: "delete",
});
export const AlertStatus = Object.freeze({ Closed: "closed", Open: "open" });
export const AlertRepeat = Object.freeze({
    Inactive: "inactive",
    EOS: "eos",
    Once: "once",
});
