// Enums for AlertItem component
export enum AlertAction {
    RESUBSCRIBE,
    CANCEL,
    DELETE,
}
export enum AlertStatus {
    CLOSED,
    OPEN,
}
export enum AlertRepeat {
    INACTIVE,
    EOS,
    ONCE,
}
