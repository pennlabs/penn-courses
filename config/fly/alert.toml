app = "penn-course-alert"
primary_region = "ewr"

[build]
  image = "pennlabs/pca-frontend"

[env]
  DOMAIN = "penn-course-alert.fly.dev"

[http_service]
  internal_port = 3000
  force_https = false # TODO: eventually true lmao
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
