app = "penn-course-landing"
primary_region = "ewr"

[build]
  image = "pennlabs/pcx-landing"

[env]
  DOMAIN = "penn-course-landing.fly.dev"

[http_service]
  internal_port = 3000
  force_https = false # TODO: eventually true lmao
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
